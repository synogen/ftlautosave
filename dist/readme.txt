# Run

ftlautosave requires Java (JRE 8) to be installed.
Put ftlautosave.jar into FTL's game folder and run it. ftlautosave.cmd is optional and can be used for watching log output.


# Configuration

On first start config.json is created, right now it has to be edited manually to change configuration options.